#!/usr/bin/env node

'use strict';

var program = require('commander');

program
  .version('0.0.0')
  .command('list', 'list current groups')
  .command('delete', 'delete specified group')
  .command('create', 'create a new group')
  .parse(process.argv);
